{templateType JulienBoudry\PhpReference\Template\Input\NamespacePageInput}
{import 'parts/namespace_tables.latte'}
{var $urlLinker = $namespaceWrapper->getUrlLinker()}
> {JulienBoudry\PhpReference\Template\Input\AbstractElementInput::getBreadcrumb($namespaceWrapper)}
{* *}
# Namespace: {$namespaceWrapper->namespace}

{if !empty($classes)}
## Classes

{include classTable, classes: $classes, urlLinker: $urlLinker}
{/if}

{if !empty($enums)}
## Enums

{include enumTable, enums: $enums, urlLinker: $urlLinker}
{/if}
