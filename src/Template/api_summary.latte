{templateType JulienBoudry\PhpReference\Template\Input\ApiSummaryInput}
{import 'classIndex.latte'}
# API Reference Index

{foreach $namespaces as $namespace => $classes}
## {$namespace}
| Class Name | Description |
| ------------- | ------------- |
{foreach $classes as $className => $class}
| [{$class->shortName}]({str_replace('\\', '/', mb_substr($class->getPagePath(), 1))}) | {$class->getDescription()} |
{/foreach}

{/foreach}